# We might support earlier versions, too, but try to use a recent version.
cmake_minimum_required (VERSION 3.8)

# Recurse into subdirectory for tests of add() and get...() functions.
add_subdirectory (add-get)
